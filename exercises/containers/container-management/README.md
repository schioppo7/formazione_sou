# Container management

Simulation of the wolf, goat, and cabbage puzzle using two virtual machines and several Docker containers.

`initial_state.sh` prepares the containers. `non_interactive_migration.sh` performs the steps and checks the rules.

`interactive_migration.sh` is a draft that still needs to be completed.
