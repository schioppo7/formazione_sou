# Disk full exercise

The `fill_disk.sh` test script simulates running out of disk space and checks command exit codes.

Warning: run it only inside a test virtual machine because it can create very large files.
