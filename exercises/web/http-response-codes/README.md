# HTTP response codes

Apache exercise covering HTTP response codes 200, 301, 403 and 404.

The `.htaccess` file contains the rules for `/old_file.html` and `/private.html`. The `http_response_output.txt` file contains the tests performed with `curl`.
