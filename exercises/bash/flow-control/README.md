# Metrics analysis

`generate_metrics.sh` creates random CPU usage values for four servers.

`analyze_metrics.sh` reads `metrics.txt` and calculates the average for each server.

`metrics.txt` contains an example of the generated data.
