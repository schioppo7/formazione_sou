# Strings exercise

The exercise starts with a fruit list containing uppercase words and duplicates.

The goal is to convert the words to lowercase, remove duplicates, and sort them.

`fruit.txt` contains the input and the Unix command example. The other solutions are in `fruit.sh` and `fruit.py`.
