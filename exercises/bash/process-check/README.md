# Process check

`process_check.sh` checks whether some processes are running and prints a final summary.

The script also creates a folder inside `/root`, so it must be run with root permissions.

The `Vagrantfile` can be used to test it in a virtual machine named `target`.
