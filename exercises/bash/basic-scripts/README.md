# Basic Bash scripts

Two small introductory Bash exercises.

- `hello_world.sh` prints a message.
- `backup_system_files.sh` tries to copy some system files to `/root`.

`backup_system_files.sh` requires root permissions.
