# Basic VM with Vagrant

Basic exercise for creating an Ubuntu virtual machine.

During provisioning, the `/home/vagrant/greeting.txt` file is created.

Main commands:

    vagrant up
    vagrant ssh
    vagrant halt
